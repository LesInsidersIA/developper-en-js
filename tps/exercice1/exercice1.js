// Importer le module 'readline'
const readline = require('readline');

// Importer le module 'lodash'
const _ = require('lodash');

const rl = readline.createInterface({
  input: process.stdin,   // Définir le flux d'entrée sur process.stdin
  output: process.stdout  // // Définir le flux de sortie sur process.stdout
});

// Crée une interface de readline pour lire les entrées utilisateur

// Demande le prénom de l'étudiant
